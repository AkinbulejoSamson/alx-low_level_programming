#include <stdio.h>

/**
 * main - Prints a sentence
 *
 * Return - 0 if exited propely , non zero if otherwise
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}

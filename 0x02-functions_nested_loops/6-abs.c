#include "main.h"

/**
 * _abs - computes absolute value
 * @n: integer to be checked
 *
 * Return: the absolute value of the int
 */

int _abs(int)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
